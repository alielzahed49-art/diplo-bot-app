<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#07071A">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#1A1A2E"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Diplomacia Token Extractor"
            android:textColor="#C8A84B"
            android:textStyle="bold"
            android:textSize="18sp"
            android:gravity="center"/>

        <ProgressBar
            android:id="@+id/progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_marginTop="10dp"
            android:max="100"
            android:progressTint="#C8A84B"
            android:visibility="gone"/>

        <TextView
            android:id="@+id/tv_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="اضغط الزرار عشان يفتح تسجيل Google جوا التطبيق"
            android:textColor="#AAAACC"
            android:textSize="13sp"
            android:gravity="center"
            android:layout_marginTop="10dp"/>
    </LinearLayout>

    <!-- الزرار الرئيسي -->
    <Button
        android:id="@+id/btn_start"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ابدأ التسجيل (Google جوا التطبيق)"
        android:textColor="#000000"
        android:backgroundTint="#C8A84B"
        android:padding="18dp"
        android:layout_margin="20dp"
        android:textStyle="bold"
        android:textSize="15sp"/>

    <!-- WebView -->
    <WebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_margin="8dp"/>
</LinearLayout>
